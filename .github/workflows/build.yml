name: Build Plugin

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Build plugin
        run: |
          sudo apt-get update
          sudo apt-get install -y make
          make dist

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v3
        with:
          name: plugin-msteams
          path: dist/plugin-msteams.tar.gz
